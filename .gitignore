# Guard: chapter/full PDFs are only committed deliberately (git add -f)
# once the neutral-cover public edition exists. Never commit the MIT
# Press-styled working draft.
pdf/*.pdf
.DS_Store
# HTML edition built from the unpublished manuscript — never commit.
html/
slides/*.pdf
